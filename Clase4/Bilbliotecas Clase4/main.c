#include <stdio.h>
#include <stdlib.h>
#include "Ingresos.h"
#include "Recursividad.h"

int main()
{
    /*int edad;
    int legajo;
    int peso;

    edad = pedirEntero("Ingrese edad: ");
    legajo = pedirEntero("Ingrese legajo: ");
    peso = pedirEntero("Ingrese peso: ");*/
    int resultado = factorial(5); //jarcodear, es poner un numero desde aca sin preguntarle al usuario

    printf("%d" , resultado);
    return 0;
}

